export type InfoProps = {
  nickname: string;
  name: string;
  email: string;
  address: string;
  url: string;
  accessToken: string | null;
};

export type QuitProps = {
  quitText: string;
  accessToken: string | null;
};
